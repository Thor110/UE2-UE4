Map Materials at /Textures/
Package Materials at /StaticMeshes/
Map StaticMeshes at /StaticMesh/
Package Materials at /Materials/




add notes on configuration defaults for umodel to the readme

or manually alter the .cfg file during execution

set default output directory

and either do or don't use "Use object groups instead of types"

to keep category names for UE2 version of the script